# ---------- Web framework ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20      # form parsing (OAuth2 login form)

# ---------- Config / validation ----------
pydantic==2.13.4              # >=2.12.5 required by google-genai 2.11
pydantic-settings==2.7.1      # reads .env into a typed Settings object
email-validator==2.2.0        # backs Pydantic's EmailStr

# ---------- Database ----------
sqlalchemy[asyncio]==2.0.36   # ORM, async engine
asyncpg==0.30.0               # async PostgreSQL driver
alembic==1.14.0               # schema migrations

# ---------- Cache ----------
redis==5.2.1                  # async Redis client (search cache)

# ---------- Auth ----------
bcrypt==5.0.0                 # password hashing (used directly; passlib is unmaintained)
python-jose[cryptography]==3.3.0  # JWT encode/decode

# ---------- Rate limiting ----------
slowapi==0.1.9                # per-route rate limits, backed by the shared Redis

# ---------- External data source ----------
httpx==0.28.1                 # async HTTP client (QuickCommerce API)

# ---------- AI layer (Parts 8) ----------
google-genai==2.11.0          # Gemini client (2.x needed for thought-signature models)
mcp==2.2.0                    # MCP server + client. 2.x is required for the
                              # Streamable HTTP transport that makes the
                              # server reachable by URL; 1.x was stdio/SSE
                              # only. Note 2.x renamed FastMCP -> MCPServer.
